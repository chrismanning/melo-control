query GetCollection($idEq: String!) {
    library {
        collections(where: {id: {EqExpr: {eq: $idEq}}}) {
            id
            name
            sourceGroups {
                sources {
                    id
                    metadata {
                        mappedTags {
                            artistName
                            trackNumber
                            trackTitle
                        }
                    }
                    downloadUri
                }
                groupTags {
                    albumArtist
                    albumTitle
                    date
                    discNumber
                    genre
                    totalDiscs
                }
            }
        }
    }
}
